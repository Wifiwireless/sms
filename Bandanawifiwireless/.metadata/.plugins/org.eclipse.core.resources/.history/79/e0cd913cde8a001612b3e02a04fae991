package com.wifiwireless.webservice;

import javax.ws.rs.GET;
import javax.ws.rs.Path;

@Path("/get")
public class Webservices {

	
	
	@GET
	@Path("/number")
	@Produces(MediaType.APPLICATION_JSON)
	public IbecconDevice getBeaconInfo(@PathParam("beaconid") String beaconid) {

		System.out.println("beaconid is " + beaconid);

		IbecconDevice device = ServiceHelper.getDeviceByUid(beaconid);

		return device;

	}
}
